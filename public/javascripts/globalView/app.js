/*
 * File: app.js
 */

Ext.Loader.setConfig({
    enabled: true
});

Ext.application({
    autoCreateViewport: true,
    name: 'BiofuelsGlobal',
  
    //--------------------------------------------------------------------------
    init: function(application) {
    },
    
    //--------------------------------------------------------------------------
    launch: function(profile) {
    }

});
